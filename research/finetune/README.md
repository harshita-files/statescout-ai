# `research/finetune` — InternVL LoRA / QLoRA

Parameter-efficient fine-tuning experiments for the perception model. Cuttable:
if this does not beat the hosted VLM baseline on the negation benchmark, it is
dropped and `perception/` keeps the provider interface it already has.
