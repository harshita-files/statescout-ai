{
  "name": "statescout-ai",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "apps/vscode-extension",
    "packages/*"
  ],
  "scripts": {
    "test": "bun test tests/unit",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'ts lint not configured yet'",
    "build": "echo 'extension build not configured yet'"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
